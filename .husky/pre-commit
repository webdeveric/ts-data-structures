branch="$(git rev-parse --abbrev-ref HEAD)"

if [ "$branch" = "main" ]; then
  echo "Error: do not commit directly to the main branch."
  echo "Please create a feature branch and open a pull request."
  exit 1
fi

pnpm exec lint-staged --relative
